# Agents

Project-specific subagent definitions. One markdown file per agent, with frontmatter
(`name`, `description`, optional `tools` and `model`) and the system prompt below it.

Put an agent here when a task needs its own isolated context and a narrower toolset —
e.g. a read-only contract auditor or a scene-performance reviewer.
