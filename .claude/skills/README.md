# Skills

Project-specific skills for Claude Code. One folder per skill, each containing a
`SKILL.md` with YAML frontmatter (`name`, `description`) and the instructions below it.

Put a skill here when a task in this repo has a repeatable multi-step procedure —
e.g. adding a contract integration, or adding a new 3D scene object.
