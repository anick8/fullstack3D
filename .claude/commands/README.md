# Commands

Project-specific slash commands. One markdown file per command; the filename is the
command name (`deploy-check.md` → `/deploy-check`). The file body is the prompt,
with optional frontmatter (`description`, `argument-hint`, `allowed-tools`).

Put a command here for a short, frequently-repeated prompt. Anything longer or
multi-step belongs in `.claude/skills/`.
